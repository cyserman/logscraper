# Core extraction engine — stdlib only, no dependencies needed

# API server
fastapi>=0.111
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# Optional document parsers
pdfplumber>=0.11       # PDF extraction
python-docx>=1.1       # .docx support
openpyxl>=3.1          # .xlsx support
